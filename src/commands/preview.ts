import { EntryType } from "../helpers/rx";
import { CmdOptionsType } from "./common";



export function previewCommand (entries: EntryType[], cmdOptions: CmdOptionsType) {
	console.log(`Running previewCommand:`);
	console.log("\t---- complete");
}
